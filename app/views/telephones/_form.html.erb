<%= form_for(@telephone) do |f| %>
  <% if @telephone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@telephone.errors.count, "error") %> prohibited this telephone from being saved:</h2>

      <ul>
      <% @telephone.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pacient_id %><br>
    <%= collection_select(:telephone, :pacient_id, Pacient.all, :id, :name,
                 {:prompt   => "Select a pacient"}) %>
</div>

  <div class="field">
    <%= f.label :ddd %><br>
    <%= f.number_field :ddd %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="field">
    <%= f.label :preferred %><br>
    <%= f.check_box :preferred %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
