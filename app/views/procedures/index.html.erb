<h1>Listing procedures</h1>

<table>
  <thead>
    <tr>
      <th>Proceduretype</th>
      <th>Insurance</th>
      <th>Name</th>
      <th>Description</th>
      <th>Value</th>
      <th>Followup</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @procedures.each do |procedure| %>
      <tr>
        <td><%= procedure.proceduretype_id %></td>
        <td><%= procedure.insurance_id %></td>
        <td><%= procedure.name %></td>
        <td><%= procedure.description %></td>
        <td><%= procedure.value %></td>
        <td><%= procedure.followup %></td>
        <td><%= link_to 'Show', procedure %></td>
        <td><%= link_to 'Edit', edit_procedure_path(procedure) %></td>
        <td><%= link_to 'Destroy', procedure, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Procedure', new_procedure_path %>
